const config = {

    port: 3001,

    database: {
        DATABASE: 'omblog_v2',
        USERNAME: 'root',
        PASSWORD: 'root',
        PORT: '3306',
        HOST: 'localhost',
        prefix: 'om_'
    }
};

module.exports = config;